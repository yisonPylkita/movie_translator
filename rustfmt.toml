# Enforce std → external → local crate import grouping.
# The justfile's `fix-rust` and `check-fmt-rust` use `cargo +nightly fmt`
# because this option is nightly-only.  CI also uses `+nightly`.
group_imports = "StdExternalCrate"
