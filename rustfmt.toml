# Stable rustfmt options only — no nightly-only features, since
# `check_fast_gate` and other tools run stable `cargo fmt --check`.
# The justfile's `fix-rust` uses `cargo +nightly fmt` for additional
# nightly-only formatting (like merge_imports).
